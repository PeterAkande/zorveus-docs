---
title: Wallets & Top-Ups API
description: Reference and interactive runner for wallets & top-ups operations.
---

# Wallets & Top-Ups

<KeyInserter type="service" label="Service Key Tester" placeholder="zrv_service_your_service_key" />

## Get Wallet

<ApiEndpoint method="GET" path="/wallet" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/wallet" authType="dashboard_session" />

---

## List Wallet Ledger

<ApiEndpoint method="GET" path="/wallet/{wallet_account_id}/ledger" auth="dashboard_session" />

### Parameters

<ParamField name="wallet_account_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ParamField name="cursor" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/wallet/{wallet_account_id}/ledger" authType="dashboard_session" />

---

## Get Top Up View

<ApiEndpoint method="GET" path="/wallet/top-up" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/wallet/top-up" authType="dashboard_session" />

---

## Get Top Up Purchase Status

<ApiEndpoint method="GET" path="/wallet/top-up/purchases/{credit_purchase_id}" auth="dashboard_session" />

### Parameters

<ParamField name="credit_purchase_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/wallet/top-up/purchases/{credit_purchase_id}" authType="dashboard_session" />

---

## Create Top Up Checkout

<ApiEndpoint method="POST" path="/wallet/top-up/checkout" auth="dashboard_session" />

<ApiRunner method="POST" path="/wallet/top-up/checkout" authType="dashboard_session" />

---

## Redeem Wallet Promotion

<ApiEndpoint method="POST" path="/wallet/promotions/redeem" auth="dashboard_session" />

<ApiRunner method="POST" path="/wallet/promotions/redeem" authType="dashboard_session" />

---

