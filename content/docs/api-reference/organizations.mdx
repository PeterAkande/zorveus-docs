---
title: Organizations & Members API
description: Reference and interactive runner for organizations & members operations.
---

# Organizations & Members

<KeyInserter type="service" label="Service Key Tester" placeholder="zrv_service_your_service_key" />

## Health

<ApiEndpoint method="GET" path="/health" auth="dashboard_session" />

<ApiRunner method="GET" path="/health" authType="dashboard_session" />

---

## Readiness

<ApiEndpoint method="GET" path="/health/readiness" auth="dashboard_session" />

<ApiRunner method="GET" path="/health/readiness" authType="dashboard_session" />

---

## Liveliness

<ApiEndpoint method="GET" path="/health/liveliness" auth="dashboard_session" />

<ApiRunner method="GET" path="/health/liveliness" authType="dashboard_session" />

---

## Complete Onboarding

<ApiEndpoint method="POST" path="/onboarding/complete" auth="dashboard_session" />

<ApiRunner method="POST" path="/onboarding/complete" authType="dashboard_session" />

---

## List Orgs

<ApiEndpoint method="GET" path="/orgs" auth="dashboard_session" />

<ApiRunner method="GET" path="/orgs" authType="dashboard_session" />

---

## Create Org

<ApiEndpoint method="POST" path="/orgs" auth="dashboard_session" />

<ApiRunner method="POST" path="/orgs" authType="dashboard_session" />

---

## Update Org

<ApiEndpoint method="PATCH" path="/orgs/{org_id}" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="PATCH" path="/orgs/{org_id}" authType="dashboard_session" />

---

## Update Quickstart

<ApiEndpoint method="PATCH" path="/orgs/{org_id}/quickstart" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="PATCH" path="/orgs/{org_id}/quickstart" authType="dashboard_session" />

---

## Get Org Members

<ApiEndpoint method="GET" path="/orgs/{org_id}/members" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="q" type="string">
  Parameter in query
</ParamField>

<ParamField name="role" type="string">
  Parameter in query
</ParamField>

<ParamField name="status" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/orgs/{org_id}/members" authType="dashboard_session" />

---

## Invite Member

<ApiEndpoint method="POST" path="/orgs/{org_id}/invites" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/orgs/{org_id}/invites" authType="dashboard_session" />

---

## Get Org Invites

<ApiEndpoint method="GET" path="/orgs/{org_id}/invites" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="q" type="string">
  Parameter in query
</ParamField>

<ParamField name="status" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/orgs/{org_id}/invites" authType="dashboard_session" />

---

## Revoke Invite

<ApiEndpoint method="DELETE" path="/orgs/{org_id}/invites/{invite_id}" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="invite_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="DELETE" path="/orgs/{org_id}/invites/{invite_id}" authType="dashboard_session" />

---

## Accept Invite

<ApiEndpoint method="POST" path="/orgs/{org_id}/invites/{invite_id}/accept" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="invite_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/orgs/{org_id}/invites/{invite_id}/accept" authType="dashboard_session" />

---

## Update Member Role

<ApiEndpoint method="PATCH" path="/orgs/{org_id}/members/{user_id}" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="user_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="PATCH" path="/orgs/{org_id}/members/{user_id}" authType="dashboard_session" />

---

## Remove Member

<ApiEndpoint method="DELETE" path="/orgs/{org_id}/members/{user_id}" auth="dashboard_session" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="user_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="DELETE" path="/orgs/{org_id}/members/{user_id}" authType="dashboard_session" />

---

## List Service Keys

<ApiEndpoint method="GET" path="/orgs/{org_id}/service-keys" auth="dashboard_session" />

List safe service-key metadata for an organization.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="GET" path="/orgs/{org_id}/service-keys" authType="dashboard_session" />

---

## Create Service Key

<ApiEndpoint method="POST" path="/orgs/{org_id}/service-keys" auth="dashboard_session" />

Create a startup management key and return its raw value once.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/orgs/{org_id}/service-keys" authType="dashboard_session" />

---

## Revoke Service Key

<ApiEndpoint method="POST" path="/orgs/{org_id}/service-keys/{org_service_api_key_id}/revoke" auth="dashboard_session" />

Revoke one organization service key.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_service_api_key_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/orgs/{org_id}/service-keys/{org_service_api_key_id}/revoke" authType="dashboard_session" />

---

## Get Personal Wallet Dashboard Overview

<ApiEndpoint method="GET" path="/dashboard-api/consumer/overview" auth="dashboard_session" />

Get the personal-org wallet home: wallet, month usage, apps, and activity.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/consumer/overview" authType="dashboard_session" />

---

## Get Organization Overview

<ApiEndpoint method="GET" path="/dashboard-api/org-overview" auth="dashboard_session" />

Get the active organization's details, wallet, and month-to-date usage.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/org-overview" authType="dashboard_session" />

---

## Get Startup Dashboard Overview

<ApiEndpoint method="GET" path="/dashboard-api/startup/overview" auth="dashboard_session" />

Get overview statistics for startup orgs.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/startup/overview" authType="dashboard_session" />

---

## Get Developer Dashboard Overview

<ApiEndpoint method="GET" path="/dashboard-api/developer/overview" auth="dashboard_session" />

Get developer-owned app summaries and privacy-safe aggregate analytics.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/developer/overview" authType="dashboard_session" />

---

## Get Developer App Dashboard Overview

<ApiEndpoint method="GET" path="/dashboard-api/developer/apps/{app_id}/overview" auth="dashboard_session" />

Get one developer app's config, review state, and aggregate analytics.

### Parameters

<ParamField name="app_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/developer/apps/{app_id}/overview" authType="dashboard_session" />

---

## Get Developer App Usage

<ApiEndpoint method="GET" path="/dashboard-api/developer/apps/{app_id}/usage" auth="dashboard_session" />

Get privacy-safe aggregate usage for one developer-owned app.

### Parameters

<ParamField name="app_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="model" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/developer/apps/{app_id}/usage" authType="dashboard_session" />

---

## Get Developer App Usage Time Series

<ApiEndpoint method="GET" path="/dashboard-api/developer/apps/{app_id}/usage/timeseries" auth="dashboard_session" />

Get privacy-safe usage buckets for one developer-owned app.

### Parameters

<ParamField name="app_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="interval" type="string">
  Parameter in query
</ParamField>

<ParamField name="model" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/developer/apps/{app_id}/usage/timeseries" authType="dashboard_session" />

---

## Get App Usage Summary

<ApiEndpoint method="GET" path="/dashboard-api/apps/{app_id}/usage" auth="dashboard_session" />

Get usage stats scoped to a specific application.

### Parameters

<ParamField name="app_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/apps/{app_id}/usage" authType="dashboard_session" />

---

