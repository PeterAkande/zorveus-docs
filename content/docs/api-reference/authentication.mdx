---
title: Auth & Current User API
description: Reference and interactive runner for auth & current user operations.
---

# Auth & Current User

<KeyInserter type="service" label="Service Key Tester" placeholder="zrv_service_your_service_key" />

## Signup

<ApiEndpoint method="POST" path="/auth/signup" auth="dashboard_session" />

<ApiRunner method="POST" path="/auth/signup" authType="dashboard_session" />

---

## Login

<ApiEndpoint method="POST" path="/auth/login" auth="dashboard_session" />

<ApiRunner method="POST" path="/auth/login" authType="dashboard_session" />

---

## Get Csrf Token

<ApiEndpoint method="GET" path="/auth/csrf" auth="dashboard_session" />

<ApiRunner method="GET" path="/auth/csrf" authType="dashboard_session" />

---

## Logout

<ApiEndpoint method="POST" path="/auth/logout" auth="dashboard_session" />

<ApiRunner method="POST" path="/auth/logout" authType="dashboard_session" />

---

## Revoke Session

<ApiEndpoint method="DELETE" path="/auth/sessions/{user_session_id}" auth="dashboard_session" />

### Parameters

<ParamField name="user_session_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="DELETE" path="/auth/sessions/{user_session_id}" authType="dashboard_session" />

---

## Revoke Other Sessions

<ApiEndpoint method="POST" path="/auth/sessions/revoke-others" auth="dashboard_session" />

<ApiRunner method="POST" path="/auth/sessions/revoke-others" authType="dashboard_session" />

---

## List Sessions

<ApiEndpoint method="GET" path="/auth/sessions" auth="dashboard_session" />

<ApiRunner method="GET" path="/auth/sessions" authType="dashboard_session" />

---

## Change Password Route

<ApiEndpoint method="POST" path="/auth/password/change" auth="dashboard_session" />

<ApiRunner method="POST" path="/auth/password/change" authType="dashboard_session" />

---

## Get Me

<ApiEndpoint method="GET" path="/me" auth="dashboard_session" />

<ApiRunner method="GET" path="/me" authType="dashboard_session" />

---

## Update Me

<ApiEndpoint method="PATCH" path="/me" auth="dashboard_session" />

<ApiRunner method="PATCH" path="/me" authType="dashboard_session" />

---

