---
title: Usage & Analytics API
description: Reference and interactive runner for usage & analytics operations.
---

# Usage & Analytics

<KeyInserter type="service" label="Service Key Tester" placeholder="zrv_service_your_service_key" />

## Get Usage Events

<ApiEndpoint method="GET" path="/dashboard-api/usage/events" auth="dashboard_session" />

List raw usage events for an organization.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="app_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="model" type="string">
  Parameter in query
</ParamField>

<ParamField name="models" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="providers" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_modes" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ParamField name="cursor" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/usage/events" authType="dashboard_session" />

---

## Get Usage Timeseries

<ApiEndpoint method="GET" path="/dashboard-api/usage/timeseries" auth="dashboard_session" />

Aggregate usage events into timeseries buckets.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="interval" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="app_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="model" type="string">
  Parameter in query
</ParamField>

<ParamField name="models" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="providers" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_modes" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/usage/timeseries" authType="dashboard_session" />

---

## Get Connected App Usage Summary

<ApiEndpoint method="GET" path="/dashboard-api/usage/app-connections" auth="dashboard_session" />

Summarize spend by app connection for an organization.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="app_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/usage/app-connections" authType="dashboard_session" />

---

## Get Usage Dashboard Summary

<ApiEndpoint method="GET" path="/dashboard-api/usage/summary" auth="dashboard_session" />

Get aggregated usage summary, top apps, and top models.

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="app_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="app_connection_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="product_end_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="external_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="model" type="string">
  Parameter in query
</ParamField>

<ParamField name="models" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="providers" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider_credential_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="billing_modes" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="member_user_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_id" type="string">
  Parameter in query
</ParamField>

<ParamField name="org_member_ids" type="string">
  Parameter in query
</ParamField>

<ParamField name="from" type="string">
  Parameter in query
</ParamField>

<ParamField name="to" type="string">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/dashboard-api/usage/summary" authType="dashboard_session" />

---

