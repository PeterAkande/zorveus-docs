---
title: Provider credentials API
description: Provider discovery, credential creation, routing priority, connection access, rotation, and deletion.
---

## List org provider credentials with service key

<ApiEndpoint method="GET" path="/provider-credentials/org-programmatic" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### Parameters

<ParamField name="q" type="string">
  Filters or selects the request by q.
</ParamField>

<ParamField name="provider" type="string">
  Filters or selects the request by provider.
</ParamField>

<ParamField name="status" type="string">
  Filters or selects the request by status.
</ParamField>

<ParamField name="routing_mode" type="string">
  Filters or selects the request by routing mode.
</ParamField>

<ParamField name="limit" type="integer" default="100">
  Filters or selects the request by limit. Default: `100`. Minimum: `1`. Maximum: `1000`.
</ParamField>

### Responses

#### 200 Successful Response

<ResponseField name="provider_credentials" type="array<object>" required>
  Returns the provider credentials value.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

## Create org provider credential with service key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### JSON body

<ParamField name="credential_name" type="string" required>
  Sets the credential name value. Minimum length: `1`.
</ParamField>

<ParamField name="default_model_policy" type="array<string>">
  Sets the default model policy value.
</ParamField>

<ParamField name="provider" type="string" required>
  Sets the provider value. Minimum length: `1`.
</ParamField>

<ParamField name="provider_config" type="object">
  Sets the provider config value.
</ParamField>

<ParamField name="routing_mode" type="string">
  Sets the routing mode value. Allowed values: `manual_grant_only`, `auto_resolve`, `disabled`.
</ParamField>

<ParamField name="routing_priority" type="integer">
  Sets the routing priority value. Default: `100`. Minimum: `0`.
</ParamField>

<ParamField name="secret" type="string" required>
  Sets the secret value. Minimum length: `1`.
</ParamField>

<ParamField name="secret_kind" type="string">
  Sets the secret kind value. Allowed values: `api_key`, `service_account_json`, `azure_ad_token`.
</ParamField>

### Responses

#### 201 Successful Response

<ResponseField name="active_secret_version_id" type="string" required>
  Returns the active secret version id value.
</ResponseField>

<ResponseField name="credential_name" type="string" required>
  Returns the credential name value.
</ResponseField>

<ResponseField name="default_model_policy" type="array<string>" required>
  Returns the default model policy value.
</ResponseField>

<ResponseField name="last_quota_exhausted_at" type="string">
  Returns the last quota exhausted at value.
</ResponseField>

<ResponseField name="last_used_at" type="string" required>
  Returns the last used at value.
</ResponseField>

<ResponseField name="last_validated_at" type="string" required>
  Returns the last validated at value.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Returns the org id value.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Returns the provider value.
</ResponseField>

<ResponseField name="provider_config" type="object" required>
  Returns the provider config value.
</ResponseField>

<ResponseField name="provider_credential_id" type="string" required>
  Returns the provider credential id value.
</ResponseField>

<ResponseField name="routing_mode" type="string" required>
  Returns the routing mode value. Allowed values: `manual_grant_only`, `auto_resolve`, `disabled`.
</ResponseField>

<ResponseField name="routing_priority" type="integer" required>
  Returns the routing priority value.
</ResponseField>

<ResponseField name="secret_fingerprint" type="string">
  Returns the secret fingerprint value.
</ResponseField>

<ResponseField name="status" type="string" required>
  Returns the status value. Allowed values: `active`, `disabled`, `deleted`.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

## Delete org provider credential with service key

<ApiEndpoint method="DELETE" path="/provider-credentials/org-programmatic/{provider_credential_id}" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Identifies the provider credential id in the request path.
</ParamField>

### Responses

#### 200 Successful Response

<ResponseField name="deleted" type="boolean" required>
  Returns the deleted value.
</ResponseField>

<ResponseField name="provider_credential" type="object" required>
  Returns the provider credential value.
</ResponseField>

<ResponseField name="retired_version_count" type="integer" required>
  Returns the retired version count value.
</ResponseField>

<ResponseField name="revoked_grant_count" type="integer" required>
  Returns the revoked grant count value.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

## Grant org provider credential to app connection with service key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/app-connections" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Identifies the provider credential id in the request path.
</ParamField>

### JSON body

<ParamField name="app_connection_id" type="string" required>
  Sets the app connection id value. Minimum length: `1`.
</ParamField>

### Responses

#### 201 Successful Response

<ResponseField name="grant" type="object" required>
  Returns the grant value.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

## Rotate org provider credential with service key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/rotate" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Identifies the provider credential id in the request path.
</ParamField>

### JSON body

<ParamField name="secret" type="string" required>
  Sets the secret value. Minimum length: `1`.
</ParamField>

<ParamField name="secret_kind" type="string">
  Sets the secret kind value. Allowed values: `api_key`, `service_account_json`, `azure_ad_token`.
</ParamField>

### Responses

#### 200 Successful Response

<ResponseField name="provider_credential" type="object" required>
  Returns the provider credential value.
</ResponseField>

<ResponseField name="rotated" type="boolean" required>
  Returns the rotated value.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

## Update org provider credential routing priority with service key

<ApiEndpoint method="PATCH" path="/provider-credentials/org-programmatic/{provider_credential_id}/routing-priority" auth="service_key" />

Send an organization service key in `Authorization: Bearer zrv_svc_...`.

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Identifies the provider credential id in the request path.
</ParamField>

### JSON body

<ParamField name="routing_priority" type="integer" required>
  Sets the routing priority value. Minimum: `0`. Maximum: `2147483647`.
</ParamField>

### Responses

#### 200 Successful Response

<ResponseField name="provider_credential" type="object" required>
  Returns the provider credential value.
</ResponseField>

#### 422 Validation Error

<ResponseField name="detail" type="array<object>">
  Returns the detail value.
</ResponseField>

---

