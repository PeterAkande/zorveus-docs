---
title: Provider Credentials API
description: Reference and interactive runner for provider credentials operations.
---

# Provider Credentials

<KeyInserter type="service" label="Service Key Tester" placeholder="zrv_service_your_service_key" />

## List Provider Credential Providers

<ApiEndpoint method="GET" path="/provider-credentials/providers" auth="service_key" />

List provider options supported by the local LiteLLM catalog.

<ApiRunner method="GET" path="/provider-credentials/providers" authType="service_key" />

---

## Create Org Provider Credential

<ApiEndpoint method="POST" path="/provider-credentials" auth="service_key" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="POST" path="/provider-credentials" authType="service_key" />

---

## List Org Provider Credentials

<ApiEndpoint method="GET" path="/provider-credentials" auth="service_key" />

### Parameters

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ParamField name="q" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="status" type="string">
  Parameter in query
</ParamField>

<ParamField name="routing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/provider-credentials" authType="service_key" />

---

## Create Org Provider Credential With Service Key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic" auth="service_key" />

<ApiRunner method="POST" path="/provider-credentials/org-programmatic" authType="service_key" />

---

## List Org Provider Credentials With Service Key

<ApiEndpoint method="GET" path="/provider-credentials/org-programmatic" auth="service_key" />

### Parameters

<ParamField name="q" type="string">
  Parameter in query
</ParamField>

<ParamField name="provider" type="string">
  Parameter in query
</ParamField>

<ParamField name="status" type="string">
  Parameter in query
</ParamField>

<ParamField name="routing_mode" type="string">
  Parameter in query
</ParamField>

<ParamField name="limit" type="integer">
  Parameter in query
</ParamField>

<ApiRunner method="GET" path="/provider-credentials/org-programmatic" authType="service_key" />

---

## Rotate Org Provider Credential With Service Key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/rotate" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/rotate" authType="service_key" />

---

## Rotate Org Provider Credential

<ApiEndpoint method="POST" path="/provider-credentials/{provider_credential_id}/rotate" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="POST" path="/provider-credentials/{provider_credential_id}/rotate" authType="service_key" />

---

## Delete Org Provider Credential With Service Key

<ApiEndpoint method="DELETE" path="/provider-credentials/org-programmatic/{provider_credential_id}" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="DELETE" path="/provider-credentials/org-programmatic/{provider_credential_id}" authType="service_key" />

---

## Delete Org Provider Credential

<ApiEndpoint method="DELETE" path="/provider-credentials/{provider_credential_id}" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="DELETE" path="/provider-credentials/{provider_credential_id}" authType="service_key" />

---

## Grant Org Provider Credential To App Connection With Service Key

<ApiEndpoint method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/app-connections" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ApiRunner method="POST" path="/provider-credentials/org-programmatic/{provider_credential_id}/app-connections" authType="service_key" />

---

## Grant Org Provider Credential To App Connection

<ApiEndpoint method="POST" path="/provider-credentials/{provider_credential_id}/app-connections" auth="service_key" />

### Parameters

<ParamField name="provider_credential_id" type="string" required>
  Parameter in path
</ParamField>

<ParamField name="org_id" type="string" required>
  Parameter in query
</ParamField>

<ApiRunner method="POST" path="/provider-credentials/{provider_credential_id}/app-connections" authType="service_key" />

---

