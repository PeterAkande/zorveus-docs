---
title: API reference overview
description: Complete REST API Reference for the Zorveus Management and Product API on api.zorveus.com.
---

The Zorveus Product and Management API provides programmatic access to manage organizations, apps, API keys, product users, and provider credentials.

---

## OpenAPI Specification

The complete, machine-readable OpenAPI 3.1 schema for all public endpoints is hosted at:

```text
https://api.zorveus.com/openapi.json
```

You can import this URL directly into **Postman**, **Insomnia**, **Swagger UI**, or code generation tools (e.g. `openapi-typescript`, `datamodel-code-generator`).

```bash
# Download the public OpenAPI spec
curl -O https://api.zorveus.com/openapi.json
```

---

## Production Servers

| Base URL | Purpose | Authentication |
| :--- | :--- | :--- |
| `https://api.zorveus.com` | Product Management API | `Authorization: Bearer zrv_svc_...` |
| `https://api.zorveus.com/v1` | OpenAI-compatible Inference Gateway | `Authorization: Bearer zrv_...` |

---

## Authentication Credentials

- **API Key (`zrv_...`)**: High-performance bearer token used in `Authorization: Bearer zrv_...` for AI completions on `https://api.zorveus.com/v1` (and native provider wire endpoints) or checking spend on `/inference-keys/usage`.
- **Service Key (`zrv_svc_...`)**: Server-to-server programmatic management secret for administrative operations on `https://api.zorveus.com` (such as provider credentials and webhooks). Service keys cannot make model inference calls.
- **Dashboard Session**: Browser session cookie with CSRF protection for web dashboard operations.

---

## API Groups

<CardGroup cols={2}>
  <Card title="Product Users & Credits" icon="layers" href="/api-reference/product-users">
    Manage customer identities, spending caps, and issue credit grants.
  </Card>
  <Card title="Provider Credentials" icon="key" href="/api-reference/provider-credentials">
    Store and configure encrypted BYOK provider keys.
  </Card>
  <Card title="Apps & Connections" icon="terminal" href="/api-reference/apps">
    Manage applications and programmatic inference connections.
  </Card>
  <Card title="Usage & Analytics" icon="activity" href="/api-reference/usage">
    Inspect key usage summaries, raw ledger events, and spending analytics.
  </Card>
</CardGroup>
